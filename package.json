{
  "name": "github",
  "version": "1.0.12",
  "description": "Adds support for gifv/webm/mp4 to discussions on github.com",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "git archive HEAD --format=zip > build.zip",
    "lint": "standard"
  },
  "author": "Quinn Rohlf <qr@qrohlf.com>",
  "license": "WTFPL",
  "private": true,
  "devDependencies": {
    "standard": "^10.0.2"
  }
}
